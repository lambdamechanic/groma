[package]
name = "groma"
version = "0.1.0"
edition = "2021"

[dependencies]
rig-core = "0.11.1" # Update to latest patch
# rig-openrouter = "0.11" # Removed - OpenRouter is part of rig-core
rig-qdrant = "0.1.12"    # Update to latest patch compatible with rig-core 0.11
qdrant-client = "1.14.0" # Update to the version used in rig-qdrant examples
clap = { version = "4.4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
sha2 = "0.10"
hex = "0.4"
walkdir = "2.4"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
futures = "0.3"
tabled = "0.15"
uuid = { version = "1.16.0", features = ["v5"] } # Update UUID and enable v5 for path hashing
url = "2.5" # For parsing Qdrant URL
